{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Mobile-only permissions (haptics).",
  "platforms": ["android", "iOS"],
  "windows": ["main"],
  "permissions": ["haptics:allow-vibrate"]
}
